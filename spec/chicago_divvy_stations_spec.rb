require "spec_helper"

describe ChicagoDivvyStations do
  it "has a version number" do
    expect(ChicagoDivvyStations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
